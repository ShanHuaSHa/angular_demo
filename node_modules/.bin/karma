#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_karma@3.0.0@karma/bin/karma" "$@"
  ret=$?
else 
  node  "$basedir/../_karma@3.0.0@karma/bin/karma" "$@"
  ret=$?
fi
exit $ret
